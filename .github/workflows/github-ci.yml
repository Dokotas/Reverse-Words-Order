name: Python packaging

on: [push]

jobs:
    lint:
				name: Update version
        runs-on: ubuntu-latest
        steps:
            - name: checkout repo
              uses: actions/checkout@v4
						- name: make
              run: make all